data_dir: data
eyes: closed
task: age4
max_per_group: null
limit_total: null
target_sfreq: 256.0
window_seconds: 4.0
batch_size: 32
domains_per_batch: 4
epochs: 40
min_epochs: 5
patience: 8
lr: 0.001
weight_decay: 0.0001
swd_weight: 0.01
validation_size: 0.2
test_size: 0.2
input_align: false
no_domain_adaptation: false
seed: 42
device: cuda
# Training-set resampling: "none" | "smote" (imblearn SMOTE on flattened EEG windows).
sampling_method: "smote"
smote_k_neighbors: 5
